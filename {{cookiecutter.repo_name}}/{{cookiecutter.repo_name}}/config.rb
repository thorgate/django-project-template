# Require any additional compass plugins here.
require 'bootstrap-sass'

# Set this to the root of your project when deployed:
http_path = "/"
css_dir = "app/src/styles/css"
sass_dir = "app/src/styles/sass"
images_dir = "app/src/styles/images"
fonts_dir = "app/src/styles/fonts"
generated_images_dir = "app/src/styles/images/generated"
javascripts_dir = ""
http_images_path = "../images"
http_generated_images_path = "../images/generated"
http_fonts_path = "../fonts"

# You can select your preferred output style here (can be overridden via the command line):
# output_style = :expanded or :nested or :compact or :compressed

# To enable relative paths to assets via compass helper functions. Uncomment:
# relative_assets = true

# To add debugging comments that display the original location of your selectors. Comment:
line_comments = false


# If you prefer the indented syntax, you might want to regenerate this
# project again passing --syntax sass, or you can uncomment this:
# preferred_syntax = :sass
# and then run:
# sass-convert -R --from scss --to sass sass scss && rm -rf sass && mv scss sass
